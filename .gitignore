# Terraform 로컬 디렉토리 (provider 플러그인 등)
**/.terraform/

# 상태 파일 (민감 정보 포함 가능)
*.tfstate
*.tfstate.backup

# 민감값이 포함된 변수 파일만 제외 (예: secret.tfvars)
*.secret.tfvars

# 크래시 로그
crash.log
crash.*.log

# 오버라이드 파일
override.tf
override.tf.json
*_override.tf
*_override.tf.json
